	dimension common(10,10),entry(3,4),read(5,6)
	logical save, stop, program
	character integer, data, endif, format(10)
	integer external,implicit, intrinsic, pause, write(5,6)
	real complex
	external complex
	data data /'!'/
	implicit = none
	assign = block
	exit = assign
	pointer = pointee
	double = precision
	precision = double
	common(9,0) = continue ** dimension
	do = double*else-end+enddo
	entry(external,implicit) = intrinsic - pause
	program = stop .and. save
	if(character .lt. close) then
		equivalence = endfile
	else if (rewind - backspace .lt. common(1,2)) then
		goto = do
		if = logical
	end if
	if( print .eq. if) then = to
	include = inquire
	open = accept
	read(5,6)   write(1,2)
	read(5,6) = write(1,2)
 6	format(i10)
	format(i10) = data
	return = real
	subroutine = then
	call call(complex,endif)
	integer = data
	end
	subroutine call (function, character)
	external function
	character character
	real = 2.3
	complex = function(real)
	return
	end
	function complex(do)
	dimension = do + 1
	external = 1
	complex = dimension/external
	return
	end
